<div class="pt-[39px] md:pt-[33px] pb-[60px]">
  <div class="w-full max-w-[1040px] mx-auto">
   <div class="flex flex-col items-stretch md:flex-row md:gap-10 {% if section.settings.layout == 'image_right' %} md:flex-row-reverse {% endif %}">
      <div class="w-full md:basis-1/2 {% if section.settings.hide_image_mobile %} hidden {% endif %} md:block">
        {% if section.settings.image != blank %}
            {% render "image" image: section.settings.image , class: "w-full h-full object-cover" %}
        {% else %}
           {{ 'image' | placeholder_svg_tag }}
        {% endif %}
      </div>
      <div class="md:basis-1/2 text-left mt-8 px-5 :px-0 md:mt-0">
        {%- if section.settings.subtitle != blank -%} 
          <p class="GroteskBold text-sm uppercase mb-2 text-chataigne">
            {{ section.settings.subtitle }}
          </p>
        {%- endif -%}

        {%- if section.settings.title != blank -%}
          <h2 class="py-2 font-[GroteskBold] text-4xl mb-6 uppercase">
            {{ section.settings.title }}
          </h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <div class="text-sm font-medium leading-[20px]">
            {{ section.settings.text }}
          </div>
        {%- endif -%} 
      </div>
    </div>
  </div>
</div>
<style>
  .section-image-text .text-base strong {
    font-family: "GroteskBold";
     font-weight: 700;
  }
</style>

{% schema %}
{
  "name": "Ebook Image",
  "settings": [
    {
      "type": "select",
      "id": "layout",
      "options": [
          {
          "value": "image_right",
          "label": "Image right"
          },
          {
          "value": "image_left",
          "label": "Image left"
          }
      ],
      "default": "image_right",
      "label": "Layout"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "checkbox",
      "id": "hide_image_mobile",
      "label": "Hide image on mobile",
      "default": false
    },
    {
      "type": "text",
      "id": "subtitle",
      "default": "NUTRILICIOUS",
      "label": "Subtitle"
    },
    {
      "type": "text",
      "id": "title",
      "default": "L’art de la nutrition",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "text",
      "default": "<p>Nos snacks sont “boostés” de nutriments, et nos compléments alimentaires sont uniques, et issus pour la plupart d’aliments bio. Comme nous avons voulu le meilleur pour nous et nos enfants, nous sommes devenues des passionnées de nutrition, accompagnées par les meilleurs experts. Nous passons des heures et des heures à éplucher les dernières recherches.</p>",
      "label": "Text"
    }
  ],
  "presets": [
    {
      "name": "Ebook Image"
    }
  ]
}
{% endschema %}